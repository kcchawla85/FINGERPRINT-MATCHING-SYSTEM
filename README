This is Fingerprint Matching System that uses a sample image and it matches all the features of the sample image with all the other images in the dataset. 
It then specifies that fingerprint is matched or not. Tech Stack used is Python
